[package]
name = "perry-ext-net"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Native bindings for Node `net.Socket` — TCP + optional TLS upgrade. Uses only `perry-ffi`. Async via spawn_blocking-per-socket. Phase 5 #466 — first net-IO wrapper port."

[lib]
crate-type = ["staticlib", "rlib"]

[dependencies]
perry-ffi.workspace = true
tokio = { workspace = true }
# Zero-copy socket reads: `read_buf` fills a reused `BytesMut` in place and
# `split_to(n).freeze()` carves each read off as a refcounted `Bytes` view,
# which the 'data' event carries to the drain handler (it only borrows it as
# `&[u8]`). Drops the per-read `Vec<u8>` alloc + memcpy. Already in the
# lockfile via tokio/rustls, so declaring it pulls nothing new.
bytes = "1"
tokio-rustls = "0.26"
rustls = "0.23"
rustls-native-certs = "0.8"

# #4914 — SO_REUSEPORT so `cluster.fork()` workers share one `net.Server`
# listen port (kernel load-balances accepts). Unix-only, matching the HTTP
# listen sites (perry-ext-http-server). Already in the workspace lock.
[target.'cfg(unix)'.dependencies]
socket2 = "0.6"

[dev-dependencies]
perry-ffi = { workspace = true, features = ["runtime-link"] }
perry-runtime.workspace = true
